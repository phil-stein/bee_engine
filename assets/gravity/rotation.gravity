extern var Entity;
extern var Game;
// extern var Input;
// extern var Camera;


func init()
{
}

func update()
{
	var this = Entity();
	var game = Game(); 

	var speed = 20 * game.get_delta_t();
	this.rotate_x(speed);
	this.rotate_y(speed);
	this.rotate_z(speed);
	
}
